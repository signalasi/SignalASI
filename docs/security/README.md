# Security

Security documents describe pairing trust, key handling, encrypted transport, local agent permissions, and data retention.

- `TRUST_MODEL.md` defines the current trust boundaries and security evidence.
